<div class="wrapper bg-white mt-sm-5">
    <form action="/profil-guncelle" method="POST" enctype="multipart/form-data">
        <% if (locals.validateError !=undefined || locals.successMessage !=undefined || locals.error !=undefined) {
            %>
            <%- include("./messages/message") %>
              <% } %>
    <div class="d-flex align-items-start py-3 border-bottom">
        <img src="/uploads/avatar/<%- user.avatar %>"
            class="img" width="150" height="150" alt="">
        <div class="pl-sm-4 pl-2" id="img-section">
            <b>Profile Photo</b>
            <p>Accepted file type .png. Less than 1MB</p>
            <input name="avatar" type="file" class="btn button border"></input>
        </div>
    </div>
    <div class="py-2">
        <div class="row py-2">
            <div class="col-md-4">
                <label for="firstname">First Name</label>
                <input type="text" name="firstName" value="<%- user.firstName  %> " class="bg-light form-control">
            </div>
            <div class="col-md-4">
                <label for="email">Last Name</label>
                <input type="text"  name="lastName" value="<%- user.lastName  %> " class="bg-light form-control" >
            </div>
            <div class="col-md-4">
                <label for="email">emaill</label>
                <input type="text" readonly name="emaill" value="<%- user.emaill  %> " class="bg-light form-control" >
            </div>
        </div>
        <div class="row py-2">
            <div class="col-md-12">
                <label for="email">Adress</label>
                <input type="text" readonly name="adress" value="<%- user.adress  %> " class="bg-light form-control" >
            </div>
        </div>
        <div class="py-3 pb-4 border-bottom">
            <button type="submit" class="btn btn-primary mr-3">Profili Güncelle</button>
            <button class="btn border button">Cancel</button>
        </div>
    </form>

    </div>
</div>